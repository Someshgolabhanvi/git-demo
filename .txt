Git
-Repository or Repos
.md - markdown
commit - used to finalize the changes






*commands*
git --version
git config --global user.name "Someshgolabhanvi"
git config --global user.email "someshsg02@gmail.com"
git config --list

1. clone and status
git clone <-somelink->
git status

->untracked - new file that git doesn't yet track
->modified - changed
->staged -file is ready to committed
->unmodified - unchanged

2.Add and Commit
add-> it's add new 0r changed files in your working directory to the git staging area
git add <-filename->
git add . ->for to add all files at a time
commit -> it is a record of change
git commit -m "some message"

3.push command
push->update local repo content to remote repo
git push
git push origin main
git push <-repo name->

4.git init command
init -> used create a new git repo 
git init
git remote add origin <-link->
git remote -v  (to verify remote)
git branch  (to check branch) 
git branch -M main (to rename branch)
git push origin main
git  push -u origin main

5.Branch Command
git branch (to check branch)
git branch -M main (to rename branch)
git checkout <-branch name-> (to navigate)
git checkout -b <-new branch name-> (to create new branch)
git checkout -d <-branch name-> (to delete branch name)

6.merging code
git diff <-branch name->  (to compare commit ,branches, files and more)
git merge <-branch name-> (to merge 2 branches)

create PR

7.pull command
git pull origin main

8.undoing changes
case 1 :staged changes
	git reset <-filename->
	git reset
case 2:commited changes(only for one changes)
	git reset HEAD~1
case 3:commited changes(for many branches)
	git reset <-commit hash->
	git reset --hard <-commit hash->